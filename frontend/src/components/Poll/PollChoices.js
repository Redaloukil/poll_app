import React from 'react';


const PollChoices = (props) => {
    return (
        <div>
            props.choices.map((choice)=>{
                <div>
                </div>
            })
        </div>
    )
}

export default PollChoices;